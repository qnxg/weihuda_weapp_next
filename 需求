现在，根据接口文档，为我编写一个移动端应用，要求：

- 你目前遍写的应用相当于一个“草稿”，即只需要确保 ui 和 ux 完善可用即可，剩余的其他方面无需特别在意。
- 你要编写的应用是一个校园类的应用，看课表是最主要的功能。应用还需要支持其他的业务，主要是各种查询，你可以参考 apifox 的接口文档
- 应用程序应该做到每个人都可以进入，但是一些需要鉴权的地方设置成需要登陆才能访问
- 你的应用需要对接口文档中所有的接口都有用到，接口中的所有响应都需要被应用使用

技术方面：

- 目前真实的后端还没有做好，因此你的数据源主要来自本地已经编写好的 mock server
    - 你在实现项目的时候如果发现需要调整 mock server，直接调整即可，mock server 主要就是能在后端尚未开发完毕之前看到应用的完整效果
- 你需要使用 React 这个框架来实现一个网页版前端。这个网页只需要适配窄屏移动设备即可，不需要考虑其他屏幕尺寸

工程方面：

- 你需要确保代码的代码质量，包括但不限于代码的可读性、可维护性、可扩展性等，代码应尽可能优雅
    - 在编写代码时注意代码简洁性，尽可能用最少的代码实现最完整的功能
    - 在编写代码时注意代码的可复用性，当你发现某个功能/组件/代码片段可以在多个地方复用时，你应该考虑将其做的通用并可复用
- 遵从 React 的最佳实践
- 采用原子化样式风格，react 的组件应该尽可能使用原子化样式，尽可能少使用内联样式。
    - 你可以借鉴 tailwindcss 的设计风格，但是项目不应该引入 tailwind
    - 项目的样式方案使用 scss，除此以外不要用其他形如 tailwindcss 的样式方案

UI 设计方面：

- 应用的 ui 设计应该符合移动端的 ui 设计规范，包括但不限于颜色、字体、图标等
- 应用的主题色（rgb）是 193,52,50
- 整个 UI 设计风格应该是扁平化且简洁的设计风格
- 应用的 ui 设计应该是全面的，你需要重点考虑如下几点：
    - 请求数据时，应该尽可能显示骨架屏或是 loading 状态，不要让用户看到白屏
    - 你需要考虑到任何接口请求出现失败的情况，并且考虑在请求失败的情况下显示友好的提示 ui（尽可能是显示错误页面，而不是弹窗或是 toast）
- 由于是移动端，你需要精心安排应用的布局，内容的排列，避免内容过密导致用户操作困难，同时考虑将重要的信息放在显眼的位置，不重要的信息放在次要的位置甚至折叠
- UI 风格要全应用统一
- 注意 UI 设计的鲁棒性，可以在后端返回各种数据（空数据，过长的数据）时，都能显示一个正常友好的界面。

AI 方面：

- 你需要寻找 ui 设计最佳实践，React 最佳实践，工程最佳实践，寻找相关 skill 自己安装
- 你需要在实现这个项目的时候注意将一些项目的设计（包括 UI 和工程方面设计）考虑，开发规范等之类的信息沉淀到 docs 文档中
- 你可以开启多个 agents 来分工，并行工作。启动多个 agents 来交叉检查彼此的代码是否满足上面的要求。
- 你需要在开工前编写一个 agents.md 文件，记录下整个项目的硬性要求，并在后续迭代过程中持续更新这些文档
- 你在实现方面有任何拿不准的地方都需要向我询问，不要自己擅作主张