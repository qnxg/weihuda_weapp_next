# 微生活移动端应用开发约束

## 产品目标

- 构建窄屏移动端优先的校园 Web 应用，课表是首要功能。
- 所有人均可进入应用；仅对依赖个人数据或写操作的页面启用登录门禁。
- 接入 Apifox 项目 `8872112` 的全部 HTTP 接口，并让每一种响应数据在界面或交互中有明确用途。
- 当前交付定位为高完成度 UI/UX 草稿；数据默认来自本地 mock server。

## 技术约束

- 使用 React、TypeScript、Vite 和 SCSS。
- 不引入 Tailwind CSS 或其他原子化 CSS 框架；在 SCSS 中维护语义化设计令牌和少量可复用原子类。
- 组件保持小而专一；共享的页面状态、请求状态和展示模式应抽成通用组件或 Hook。
- 独立请求并行发起，避免请求瀑布；只有存在明确的参数、鉴权或业务顺序依赖时才允许串行等待；非首屏重模块按路由懒加载。
- 多请求页面不得把互不依赖的 Query 聚合成全页 `loading/error` 提前返回；每个请求的加载、失败和重试必须限制在消费该数据的最小 UI 区块，静态页面壳、其他成功区块及不依赖接口的本地信息必须持续可用。
- API 类型、请求方法和业务页面之间必须可追踪，不允许散落裸 `fetch` 调用。

## 视觉与交互

- 主题色固定为 `rgb(193, 52, 50)`，整体扁平、克制、清晰。
- 只适配窄屏移动设备；主内容最大宽度固定，兼容安全区与小屏滚动。
- 使用统一字体层级、8px 以内圆角、44px 以上可点击区域和 Lucide 图标。
- 页面不得依赖纯色差表达状态；表单必须有可见标签和可读错误信息。
- 所有数据页面必须提供与影响范围匹配的加载骨架、错误恢复、空状态和超长文本处理；仅当页面核心内容确实全部依赖同一失败数据源时使用页面级错误。
- 禁止用 toast 或弹窗代替主要错误状态；破坏性操作必须二次确认。
- 底部导航固定为高频入口，低频业务统一从“服务”页进入。

## 数据与鉴权

- 公共能力可匿名使用；个人数据、设置与所有写操作要求登录。
- token 由统一鉴权模块持久化，遇到 `401` 尝试刷新一次，失败后清理会话并跳转登录。
- mock server 必须保留延迟和强制错误注入能力，以验证 loading 与错误 UI。
- 所有 API 响应必须经过统一 envelope 解析；二进制图片接口单独处理。

## 质量门槛

- TypeScript 开启严格模式；ESLint 不允许遗留错误。
- 核心数据适配器、鉴权门禁、错误状态和关键交互必须有自动化测试。
- mock server 测试需校验接口总数及关键有状态流程。
- 完成后运行构建、单元测试、接口测试和 Playwright 移动端验收。
- 在至少 `375x812` 与 `390x844` 两个视口检查：无横向滚动、无文本遮挡、无空白画布、底部导航不覆盖内容。
- 由多个独立 agent 分别检查接口覆盖、React 工程质量和移动端 UI/UX，并修复已确认问题。

## 文档维护

- `docs/product.md` 记录信息架构、鉴权边界和接口到界面的映射。
- `docs/design-system.md` 记录视觉令牌、组件状态及移动端布局规则。
- `docs/engineering.md` 记录目录结构、数据流、测试策略和本地开发方式。
- 接口、页面或约束变化时，同步更新上述文档与本文件。

## 已落地决策

- 一级导航固定为“今日、课表、服务、我的”；受保护服务从公开目录进入时跳转登录并保留返回地址。
- 登录接口的 `code` 明确为微信临时 code；短信验证码由独立 `/tfa` 流程处理。
- session version 防止旧刷新请求覆盖新账号；登录、退出和自动失效时清理用户查询缓存。
- mock 默认开启 `500–1000ms` 抖动和 Bearer 鉴权，可用 `X-Mock-Delay: 0` 关闭单次延迟；`GET /countdown` 可用 `X-Mock-Date` 固定测试日期。
- 首页严格消费 `index_card` 设置；卡片 key、实验绑定编码和校园卡筛选值与 Apifox 契约保持一致。
- 首页匿名态与登录态保持相同的日期标题栏和内容顺序；公开数据直接展示，依赖受保护接口的区块在原位置提供登录说明与按钮，且匿名时不得发起这些请求。
- 未登录门禁与页面错误状态一样开放展示，不使用卡片、阴影或图标底色；操作统一为主色下划线文本按钮“去登录”。
- 首页顶部展示本地月日与星期，通知入口置于左侧，副标题通过公开 `GET /semester` 展示学年学期并加粗当前周次；标题栏吸顶并显示轻量阴影。
- 首页课程日期标签栏与标题栏保持 `8px` 间距；登录与匿名态使用同一套可点击和键盘切换的今日/明日 Tab，匿名态只将面板内容替换为对应日期的登录门禁；课程合并连续节次并支持今日/明日切换，同一天的课程 item 共用一个外层卡片和一层阴影，item 分割线仅绘制在内容区且不切断侧边条；未开始使用默认白色表面和中性侧边条，已结束使用中性弱化样式和更浅侧边条，即将开始使用 warning 色，上课中使用 success 色，两者在卡片右侧显示提示文字，全部状态文案均保留在无障碍名称中；今日课程全部结束后默认显示明日。
- 首页课程后固定展示“倒计时”区块，不扩展 Apifox `index_card` 枚举；假期和学期倒计时统一来自公开 `GET /countdown`，前端不得维护节假日表或自行推导剩余天数；首页通过公开 `GET /semester` 获取当前学期，登录后再以其学年学期加载课表与成绩。
- 首页在倒计时后固定展示校园网摘要，不扩展 Apifox `index_card` 枚举；登录态通过 `GET /netflow` 同时展示本月已用流量、免费流量占比、免费流量、可选的超额流量、锁定状态与欠费金额，进度超过 50% 使用 warning 色、超过 90% 使用主色；匿名态仅显示原位登录门禁且不得发起请求。
- 课表页标题栏左侧依次提供添加课程和 `CalendarSync` 学期切换图标按钮，副标题显示“学年 + 学期”；学期选择弹窗通过 `xn/xq` 切换当前及历史学期，列表项标题为“学年 + 学期”、副标题为日期范围，仅当前学期显示“当前”，选中项只使用主题浅底且不显示“已选”或对勾。弹窗固定标题栏、正文可滚动并支持遮罩关闭，历史学期默认从首周展示。周次使用左对齐普通标题，旁边显示学期总周数，上一周和下一周按钮并排置于最右侧且在首周、末周分别禁用；周次行保留 44px 触控高度并压缩上下留白。星期栏滚动到标题栏下方后共同吸顶并向上重叠 1px 消除接缝，标题栏阴影过渡到星期栏下方，副标题动画补充当前查看周次。课表使用周日至周六和第 1 至第 12 节的完整周网格，连续节次合并为跨行课程块，点击课程打开完整详情，非本周课程以中性斜纹和文字同时标记。星期表头下方是课表内唯一分割线，所有空格为白色且不使用整列高亮；仅当前学期当前周的今天表头保留主题色文字与 indicator；课程块仅使用极轻阴影。`375px` 及以上视口完整展示七天，更小视口仅允许课表容器内部横向滚动。
- 服务页图标统一使用品牌深色；匿名态不显示服务入口锁图标，受保护入口仍由路由门禁跳转登录并保留返回地址。
- 服务页将“课程成绩”和“成绩排名”作为两个独立功能；课程成绩页标题栏不显示副标题，成绩区标题显示当前学年学期并在右侧提供切换按钮，通过与课表页共享的学期选择弹窗切换秋、冬、春、夏学期，课程 item 之间使用分割线；成绩排名页完整开放教务排名接口的学年、全部或秋、春、夏学期、课程范围、数据来源和成绩取值筛选，并独立展示可信凭证排名。排名结果按课程类型使用“指标列 + 成绩/排名行”矩阵，只渲染实际存在的数据行；同一课程类型的成绩和排名字段各自成组存在，二者均缺失时显示明确空状态。mock 为每个课程类型独立随机生成“成绩和排名都有、仅成绩、仅排名、均无”四种状态。
- 移动端主内容使用独立滚动容器，固定底栏不覆盖内容；路由切换同步标题并聚焦页面标题。
